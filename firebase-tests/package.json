{
  "name": "aktivite-firebase-tests",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "firebase emulators:exec --project nar-rehberi --only firestore,storage \"node --test firestore.rules.test.js storage.rules.test.js\"",
    "test:static": "node --check firestore.rules.test.js && node --check storage.rules.test.js && node --check helpers/runtime.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.12.0"
  }
}
